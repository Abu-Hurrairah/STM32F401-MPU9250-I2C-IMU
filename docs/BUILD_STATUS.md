# Build Status

The archived STM32CubeIDE project record for `STM32F401CE` reported:

```text
Errors:   0
Warnings: 0
```

This status applies to the preserved original project.

The cleaned driver under `firmware/` was prepared as a reusable version of the same I²C/register workflow. It was not executed against real STM32F401 + MPU9250 hardware while this GitHub package was prepared.
